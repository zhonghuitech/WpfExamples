<Window x:Class="WindowCapture.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WindowCapture"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <Style x:Key="ImageEnabled" TargetType="Image">
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.25"></Setter>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Grid>
        <WrapPanel>
            <Button x:Name="ScreenRecordRectButton" Grid.Column="2" Width="30" Height="30"
                HorizontalAlignment="Right" 
                Click="ScreenRecordRectCallback"  ToolTip="录屏区域选择" Background="Black">
                <Image Source="\Resources\screencut.png" Style="{StaticResource ImageEnabled}" />
            </Button>
        </WrapPanel>
    </Grid>
</Window>
